<h1>2000K</h1>

<% if logged_in? %>

  <p>Hi, <%= current_user.user_name %>!</p><br>
  <%= link_to "Logout", logout_path %>

  <% if current_user.is_admin %>
    <strong>Admin things</strong>:
  <% end %>

<% else %>

<%= link_to "Sign Up!", new_user_path %>
<%= link_to "Login", login_path %>

<% end %>
