<h1>2000K</h1>

<% if logged_in? %>

  <p>Hi, <%= current_user.name %>!</p><br>
  <%= link_to "logout", logout_path %>

  <% if current_user.admin %>
    <strong>Admin things</strong>:
  <% end %>

<% else %>

<%= link_to "Sign Up!", new_user_path %>
<%= link_to "Login", session_new_path %>

<% end %>
